Fix patch:
- Sales: rerenders editable fields after Azure user role is loaded and saves inline edits.
- Product pages: replaces weak detection with robust active-section editing for Identification, Application, Brands, Suppliers.
- Product edits save per product + section to Azure storage and remain after reload.
